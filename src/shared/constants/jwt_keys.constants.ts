export const jwtKeysConstants = {
  secret_access_token_key: process.env.SECRET_ACCESS_TOKEN_KEY,
  secret_refresh_token_key: process.env.SECRET_REFRESH_TOKEN_KEY,
  secret_recovery_password_token_key:
    process.env.SECRET_RECOVERY_PASSWORD_TOKEN_KEY,
};
